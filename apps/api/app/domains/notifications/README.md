# Notification Domain

Use a provider abstraction for SMS / WhatsApp / email.

The MVP can start with an internal notification/log adapter for development.

Do not hard-code a gateway provider into quote business logic.
