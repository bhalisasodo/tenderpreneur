# Shared Contracts

Keep API-facing types/schemas here when sharing them between frontend and backend is practical.

Do not duplicate business rules in TypeScript. The backend remains authoritative for validation and authorization.
